\ProvidesPackage{preamble}

\usepackage[english,ngerman,german]{babel}% English and new German spelling
\usepackage[utf8]{inputenc}% correct input encoding
\usepackage[T1]{fontenc}% correct output encoding
\usepackage{graphicx}% enhanced support for graphics
\usepackage{tabularx}% more flexible tabular
\usepackage{amsfonts}	% math fonts
\usepackage{amssymb}	%math symbols
\usepackage{amsmath}	% overall enhancements to math environment
\usepackage{amsthm}%math theorems
\usepackage{booktabs} % for much better looking tables
\usepackage{array} % for better arrays (eg matrices) in maths
\usepackage{paralist} % very flexible & customisable lists (eg. enumerate/itemize, etc.)
\usepackage{verbatim} % adds environment for commenting out blocks of text & for better verbatim
\usepackage{subfig} % make it possible to include more than one captioned figure/table in a single float
\usepackage{color}%colors
\usepackage[numbers]{natbib} % for citeauthor
\usepackage[normalem]{ulem}
\usepackage{hyperref}
\usepackage[yyyymmdd]{datetime} % for writing dates
\usepackage{float} % force float to position with H
\usepackage[framemethod=tikz]{mdframed} % frame around figures
\usepackage{multicol} % multicolumn itemize and enumerate
\usepackage{wrapfig}
\usepackage[bottom]{footmisc}

\raggedbottom

% fullcite/1: cite both author and numeric code of reference
\newcommand{\fullcite}[1]{\citeauthor{#1} \cite{#1}}

% taxonomy or concept hierarchy
\newcommand{\taxon}{\ensuremath{\le_C}}
% relation signature
\newcommand{\relsig}{\ensuremath{\sigma_R}}
% relation hierarchy
\newcommand{\relhier}{\ensuremath{\le_R}}
% attribute signature
\newcommand{\attsig}{\ensuremath{\sigma_\mathcal{A}}}
% root class
\newcommand{\rootc}{\ensuremath{root_C}}
% subclass of relation
\newcommand{\subclassof}[1]{
\ensuremath{\vartriangleleft_{#1}}
}

% absolute value
\newcommand{\abs}[1]{\lvert #1 \rvert}
% norm
\newcommand{\norm}[1]{\left\lVert#1\right\rVert}
% projection
\newcommand{\proj}[2]{\pi_#1(#2)}

% date of dump
\newcommand{\dumpdate}{\formatdate{7}{11}{2016}}
% entity
\newcommand{\entity}{\textit{entity (Q35120)}}

\theoremstyle{definition}
\newtheorem{definition}{Definition}
\newtheorem*{definition*}{Definition}
\newtheorem{algorithm}{Algorithm}

% argmin (https://tex.stackexchange.com/a/5255)
\DeclareMathOperator*{\argmin}{arg\,min}

% Roman numerals (https://tex.stackexchange.com/a/23492)
\newcommand*{\rom}[1]{\expandafter\@slowromancap\romannumeral #1@}